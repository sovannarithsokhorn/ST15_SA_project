services:
  - type: web
    name: st15-sa-project
    runtime: php
    phpVersion: 8.2  # ← Critical addition
    buildCommand: bash render-build.sh
    startCommand: php artisan serve --host=0.0.0.0 --port=10000